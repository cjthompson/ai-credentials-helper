# Concise Project README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a concise, user-facing `README.md` for installing, using, transferring, and testing Claude Code and Codex CLI credentials.

**Architecture:** Create one root-level Markdown document derived from the current installer and CLI help. Keep operational guidance near runnable examples, and separate credential-safety warnings from ordinary usage so readers can find both quickly.

**Tech Stack:** Markdown, Python 3.12+, `uv` or `venv`, pytest, Ruff

## Global Constraints

- Keep the document concise and task-oriented.
- Use commands supported by the current CLI.
- State that Claude is the default backend and Codex requires `--agent codex`.
- Explain that `--send` and `--receive` require the same passphrase.
- Do not include real credentials, personal information, badges, screenshots, or speculative features.
- Avoid duplicating detailed implementation documentation.

---

### Task 1: Add and validate the project README

**Files:**
- Create: `README.md`
- Reference: `install.py`
- Reference: `ai_credentials_helper/cli.py`
- Reference: `pyproject.toml`
- Reference: `tests/test_cli_live_transfer.py`

**Interfaces:**
- Consumes: `python install.py`, the `credentials-helper` console entry point, and CLI options from `credentials-helper --help`.
- Produces: A root-level README that is the primary onboarding document for users and contributors.

- [ ] **Step 1: Create the concise README**

Write `README.md` with:

- A one-paragraph description of the two supported backends: Claude Code in macOS Keychain and Codex CLI in `~/.codex/auth.json`.
- Requirements: macOS and Python 3.12+, with `uv` recommended.
- Installation using `python3 install.py`, including the `~/.local/bin` PATH note.
- Common examples for `--simple`, `--raw`, `--oauth-only`, `--import`, `--refresh`, and `--force`, distinguishing Claude defaults from Codex opt-in behavior.
- Receiver-first encrypted transfer examples using `CLAUDE_CREDENTIALS_PASSPHRASE`, `--receive`, and `--send`.
- Security notes that commands can print credentials, transfer encryption depends on a shared passphrase, Codex writes use `0600`, and tests use synthetic temporary credentials.
- Development commands:

```bash
.venv/bin/pytest -q
.venv/bin/ruff check .
```

- [ ] **Step 2: Validate documented CLI options**

Run:

```bash
.venv/bin/credentials-helper --help
```

Expected: every option shown in README examples appears in the CLI help, Claude is the default agent, and Codex is an allowed `--agent` value.

- [ ] **Step 3: Validate the documented test commands**

Run:

```bash
.venv/bin/pytest -q
.venv/bin/ruff check .
```

Expected: pytest exits successfully, with the localhost test allowed to skip when sandbox sockets are unavailable; Ruff prints `All checks passed!`.

- [ ] **Step 4: Review Markdown and sensitive content**

Run:

```bash
rg -n 'TBD|TODO|PLACEHOLDER|/Users/|chris@|sk-[A-Za-z0-9_-]{20,}' README.md
git diff --check
```

Expected: the README scan returns no matches and `git diff --check` exits successfully.

- [ ] **Step 5: Commit the README and plan**

```bash
git add README.md docs/superpowers/plans/2026-07-13-readme.md
git commit -m "docs: add project readme"
```
